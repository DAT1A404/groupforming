#include <stdio.h>
#include <stdlib.h>

void show_commands(group *grps, int groupCount, int debug);
void print_not_implemented();
void see_group(group *grps, int _GroupCount);
void see_person(group *grps, int _GroupCount);